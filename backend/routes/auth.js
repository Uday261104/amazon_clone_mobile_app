const express=require("express");
const { model } = require("mongoose");

const authRouter=express.Router();

authRouter.post('/api/sign-up',(res,res)=>{
    //Recieve Data
    //validation
    //Upload the data db

    
})

model.exports=authRouter;